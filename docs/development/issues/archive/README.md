# Archived issues

Resolved upstream issues that originated from agnostik audits / refreshes.
Files move here once the underlying bug is fixed in a shipped Cyrius release
and verified live in agnostik. The original report is preserved for audit
lineage; each file's header carries the resolution version + verification
note.

Active issues live in the parent directory.
